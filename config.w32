// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("slime", "for slime support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("slime", "enable slime support", "no");

if (PHP_LEDU != "no") {
	EXTENSION("slime", "slime.c");
}

